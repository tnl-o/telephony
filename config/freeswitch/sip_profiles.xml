<!-- 
  ЭТОТ ФАЙЛ НЕ ИСПОЛЬЗУЕТСЯ — FreeSWITCH читает конфигурацию из Docker image.
  Реальная конфигурация: /etc/freeswitch/sip_profiles/internal.xml (в контейнере)
  См. freeswitch/conf/sip_profiles/internal.xml в репозитории.
  
  Для изменения настроек SIP — правьте freeswitch/conf/sip_profiles/internal.xml
-->
<profiles>
  <!-- Конфигурация в Docker image -- freeswitch/conf/sip_profiles/internal.xml -->
</profiles>
